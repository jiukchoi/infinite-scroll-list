import { css } from '@emotion/react';

export const reset = css`
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  };
  html, body, #root {
    width: 100%;
    height: 100%;
  };
`;
